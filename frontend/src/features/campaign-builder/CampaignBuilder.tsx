import { ScreenPlaceholder } from "@/shared/components/ScreenPlaceholder";

/** PRD Section 21.8 — Campaign Builder. Real implementation lands in ADS-03. */
export function CampaignBuilder() {
  return <ScreenPlaceholder title="21.8 Campaign Builder" builtBy="ADS-03" />;
}
